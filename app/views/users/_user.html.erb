<li>
  <%= gravatar_for user, size: Settings.user.gravatar.size %>
  <%= link_to user.name, user %>
  <% if current_user.is_admin? && !current_user?(user) %>
    | <%= link_to t("user.index.delete"), user, method: :delete, data: {confirm: t("user.index.confirm")} %>
  <% end %>
</li>
